 <% if(success && success.length){ %> 
    <div class="alert alert-info alert-dismissible fade show col-12 col-md-6 mx-auto" role="alert"> 
    <%= success %>
     
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>

    </div>
<%  } %>

<%  if(error && error.length){   %>

    <div class="alert alert-danger alert-dismissible fade show col-12 col-md-6 mx-auto" role="alert"> 
        <%= error %>
         <!-- here or above error and success, we're accessing res.locals.error or success -->
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    
        </div>


    <% } %>

<!-- every request no matter will go throw our that middleware and our res.locals.success so our success array (as req.flash creates array for a key (eg: "success")), so evenn if any success msg is not there but still success array will exist so success.length  and same for error-->